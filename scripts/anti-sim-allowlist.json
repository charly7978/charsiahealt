{
  "$schema": "Centralized allowlist for anti-simulation guardrail. Every entry MUST include reason and ref (issue/PR id). Set `dist: true` for entries that apply to the built bundle scanner.",
  "entries": [
    {
      "file": "src\\render\\ecg\\EcgParticles.ts",
      "pattern": "MATH_RANDOM",
      "reason": "Cosmetic-only THREE.Points blood-cell flow uses Math.random for visual spawn jitter/phase. No medical measurement, vital sign or PPG signal value derives from it; the PPG channel consumes only real sensor values. Verified: cannot reach src/modules/signal-processing/** or src/modules/vital-signs/**.",
      "ref": "GUARDRAIL-VISUAL-RENDER"
    },
    {
      "dist": true,
      "pattern": "MATH_RANDOM",
      "reason": "React internals (event listener key, fiber id), @supabase localStorage probe and three.js internal utilities use Math.random for non-medical, non-signal-path identifier/generation. Verified: zero references reach src/modules/signal-processing/** or src/modules/vital-signs/**.",
      "ref": "GUARDRAIL-DIST-RUNTIME"
    }
  ]
}
